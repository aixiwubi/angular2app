import {AddressSearchComponent} from "./addressSearch.component";
import {RouterModule} from "@angular/router";
const routes = [
    {path: '', component: AddressSearchComponent},
   
];

export default RouterModule.forChild(routes);